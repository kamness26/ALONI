name: ALONI Auto Booking

on:
  schedule:
    - cron: '0 4 * * *'   # runs daily at midnight EST (4 AM UTC)
  workflow_dispatch:       # allows manual runs from the Actions tab
    inputs:
      execute_booking:
        description: "Set true to actually click BOOK (false = dry run)"
        required: false
        default: "false"

jobs:
  run-aloni:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      COREPOWER_EMAIL: ${{ secrets.COREPOWER_EMAIL }}
      COREPOWER_PASSWORD: ${{ secrets.COREPOWER_PASSWORD }}
      CI: "true"
      EXECUTE_BOOKING: ${{ github.event_name == 'schedule' && 'true' || (github.event_name == 'workflow_dispatch' && inputs.execute_booking || 'false') }}

    steps:
      - name: 🧘 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      # Run under a virtual X server so the site fully hydrates & renders buttons
      - name: 🚀 Run ALONI booking script (with xvfb)
        run: |
          echo "🧘 Starting ALONI automation..."
          xvfb-run --auto-servernum --server-args='-screen 0 1280x800x24' \
            python scripts/book_class_verified_functional.py

      # Always upload artifacts so we can see what CI saw (trace/video/screens)
      - name: 📸 Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: |
            trace.zip
            videos/
            screenshots/
